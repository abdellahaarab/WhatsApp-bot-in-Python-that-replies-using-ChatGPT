# WhatsApp-bot-in-Python-that-replies-using-ChatGPT
We’ll build a WhatsApp bot that:  Uses Twilio’s WhatsApp API to receive and send messages.  Uses OpenAI’s ChatGPT API (GPT-5) to generate replies.  Runs with Flask (a small web server).
